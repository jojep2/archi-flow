{
  "name": "archiflow-land-use-mvp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:static",
    "build:next": "next build",
    "build:static": "mkdir -p dist && cp index.html main.js style.css login.html dist/",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.0",
    "next": "^15.3.0",
    "openai": "^4.104.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "@types/react": "^19.0.12",
    "@types/react-dom": "^19.0.4",
    "typescript": "^5.8.3"
  }
}
